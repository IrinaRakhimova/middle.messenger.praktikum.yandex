{
  "name": "chat",
  "version": "1.0.0",
  "scripts": {
    "prebuild": "node precompile.mjs",
    "build": "npm run prebuild && vite build",
    "dev": "vite",
    "start": "vite preview"
  },
  "dependencies": {
    "handlebars": "^4.7.8"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
