{
  "name": "chat",
  "version": "1.0.0",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "prebuild": "node precompile.mjs",
    "build": "npm run prebuild && vite build",
    "dev": "vite",
    "start": "vite preview --port 3000"
  },
  "dependencies": {
    "core-js": "^3.43.0",
    "handlebars": "^4.7.8"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.6",
    "postcss-preset-env": "^10.2.4",
    "vite": "^7.0.0"
  }
}
